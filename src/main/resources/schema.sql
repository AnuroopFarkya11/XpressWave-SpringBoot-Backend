CREATE TABLE PMS_USERS(ID INT PRIMARY KEY, FULL_NAME VARCHAR(255) NOT NULL, EMAIL VARCHAR(255) NOT NULL, MOBILE VARCHAR(255) NOT NULL, PASSWORD VARCHAR(255) NOT NULL, ADDRESS VARCHAR(255), ROLE VARCHAR(10) NOT NULL)